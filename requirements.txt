############################################################
# Core game + server runtime dependencies                  #
# (Only direct deps; transitive ones are resolved by pip)  #
############################################################

pyglet==2.1.12            # rendering
pymunk==7.1.0             # physics

fastapi==0.115.8          # API server
uvicorn==0.33.0           # ASGI server

numpy==1.26.4             # numeric helpers
Pillow==10.4.0            # image handling
pycairo==1.26.1           # cairo (import as cairo)
xmlschema==3.4.3          # CML validation

# The editor & chemistry stack lives in requirements-cmleditor.txt2
# rdkit / PyGObject / requests / bs4 kept out of core runtime

